"""Fallback guarantees live in the Gearbox dispatcher itself.

This package will hold fallback-specific diagnostics (e.g. counters of why
calls fell back) once telemetry lands. Kept as a named module so the spec's
component map stays navigable.
"""
